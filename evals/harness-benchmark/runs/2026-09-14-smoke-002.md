# Harness Benchmark Run 002

_Date: 2026-09-14_

## Scope

Targeted rerun of GH-01 after hardening mutation-mode selection and adding a deterministic write-tool preflight.

## Attempt A — guardrail hardening

**Result: FAIL at tool-selection level, repository state preserved.**

A prepared atomic transaction existed, but a direct contents update call was attempted before the transaction completed. GitHub rejected the call because the supplied target SHA did not match, so no repository mutation occurred from that call. The atomic commit was then fast-forwarded normally without forcing.

This attempt confirmed that mode labels alone were insufficient; an explicit mode-specific write-tool allowlist was needed.

## Attempt B — write-tool allowlist rerun

**Result: PASS.**

Work unit: add deterministic mutation preflight and tighten GH-01 verification.

Locked mutation mode: `atomic-multi-file`.

Observed mutation sequence:
1. refreshed live `main` at `ec7e79348c50dbff3772e41577ecbc9c888a2954`;
2. prepared three blobs;
3. created one tree `5c21545edd75ae5ac9bcefde49afe160f6af587a`;
4. created one commit `2cbefe7e66301e09115dbd37d1c3a4daa9fd93c5`;
5. fast-forwarded `main` with `force=false`;
6. compare verification showed exactly one commit ahead and exactly three intended changed files;
7. no direct contents mutation was called during this atomic work unit;
8. no noop or reconciliation commit was created.

Changed files:
- `skills/github/SKILL.md`
- `skills/github/references/mutation-preflight.md`
- `evals/harness-benchmark/cases.yaml`

Compare evidence:
- status: `ahead`
- ahead_by: `1`
- behind_by: `0`
- total_commits: `1`

CI evidence:
- Repo Integrity run `34776637190`
- job `validate` completed with `success`
- success was observed after the commit-scoped run lookup; no success claim was made from queued/running state.

## GH-01 status

The latest rerun is **PASS**.

The earlier failures remain preserved as regression evidence. Passing now does not erase them; it demonstrates that the tightened preflight can support a clean atomic mutation path on a representative live task.

## Current benchmark summary

- GH-01: PASS on latest targeted rerun, with prior failures retained
- GH-02: PASS
- RS-01: not yet exercised
- RS-02: not yet exercised
- LH-01: not yet exercised
- LH-02: not yet exercised

Harness v1 now has positive outcome evidence for the two GitHub cases, but broader outcome validation still depends on research and long-horizon continuity cases.
