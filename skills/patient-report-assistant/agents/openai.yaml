interface:
  display_name: "Patient Report Assistant"
  short_description: "Enter and verify monthly OPD/IPD report data"
  default_prompt: "Use $patient-report-assistant to transcribe OPD/IPD monthly paper forms into the authorized workbook, verify derived report values, and identify manual-only fields that need review."

policy:
  allow_implicit_invocation: false
